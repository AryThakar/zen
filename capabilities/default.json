{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The embedded Zen interface: notifications and its own engine commands.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
